# Base URL of the FastAPI backend (inlined at `npm run build` / Docker build).
# Local dev: http://localhost:8000
# Production: https://api.arbitcrypto.xyz (set via CI build-arg in build-and-push.yml)
NEXT_PUBLIC_API_URL=http://localhost:8000
